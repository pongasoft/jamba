@Echo OFF

set CONFIG=Debug
IF not [%1]==[] set CONFIG=%1

cmake --build . --config %CONFIG% --target ${target}
cmake --build . --config %CONFIG% --target validator
./bin/validator VST3/${target}.vst3
