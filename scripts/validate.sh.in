#!/usr/bin/env bash

CONFIG=${1:-"@JAMBA_DEFAULT_CONFIG@"}

cmake --build . --config ${CONFIG} --target @target@
cmake --build . --config ${CONFIG} --target validator
./bin/${CONFIG}/validator VST3/${CONFIG}/@target@.vst3
